// Placeholder — full widget tests require a Hive + SharedPreferences test harness.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () => expect(true, isTrue));
}
